<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/primestack_primary"
        android:text="Tap to Pay"
        android:textColor="@color/white"
        android:textSize="18sp"
        android:textStyle="bold"
        android:padding="16dp"
        android:gravity="center"
        android:layout_marginBottom="48dp" />

    <ImageView
        android:id="@+id/ivNfcIcon"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@android:drawable/ic_menu_view"
        android:contentDescription="NFC Icon" />

    <TextView
        android:id="@+id/tvTapInstruction"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Please tap card on the back of device."
        android:textColor="@color/primestack_secondary"
        android:textSize="16sp"
        android:gravity="center"
        android:layout_marginTop="32dp" />

    <Button
        android:id="@+id/btnCancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cancel"
        android:textSize="14sp"
        android:padding="12dp"
        app:backgroundTint="@color/primestack_light_gray"
        android:textColor="@color/primestack_secondary"
        android:layout_marginTop="32dp" />

</LinearLayout>
